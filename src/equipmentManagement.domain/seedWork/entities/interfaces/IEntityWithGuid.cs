using equipmentManagement.domain.seedWork.objectValues;

namespace equipmentManagement.domain.seedWork.entities.interfaces
{
    public interface IEntityWithGuid : IEntity<EntityIdentity>
    {
    }
}